-- 3) 21, 26, 52, 53, 59, 2.2 

-- 26. Задано натуральное число k, найти все натуральные решения (x, y) уравнения 𝑥^2 + 𝑦^2 = 𝑘^2. Результат вернуть в виде списка кортежей. Решения (2, 3) и (3, 2) можно считать равными или различными, как будет удобнее.
-- Функция для поиска всех пар (x, y)
-- k - натуральное число (гипотенуза)
-- Возвращает список кортежей (x, y), натуральных решений уравнения x^2 + y^2 = k^2
findSolutions :: Int -> [(Int, Int)]
findSolutions k = 
    -- Перебираем x от 1 до k, y от 1 до k
    [ (x, y) 
    | x <- [1..k]
    , y <- [1..k]
    , x*x + y*y == k*k    -- Условие уравнения
    ]

-- Главная функция для примера
main :: IO ()
main = do
    let k = 5  -- Пример значения k
    let sols = findSolutions k
    putStrLn $ "Решения уравнения x^2 + y^2 = " ++ show k ++ "^2:"
    -- Вывести все найденные пары
    mapM_ print sols
